# Forward Observation Conformance Input Bundle V1

This companion contract carries the minimum deterministic numeric inputs needed to reproduce golden strategy targets independently of an operational provider. It is linked to an immutable Standard V1 handoff but does not change `forward_observation_handoff_standard_v1:1`.

Conformance inputs are software-test references. They are not a live feed, operational market data, or permission to calculate a current target.
