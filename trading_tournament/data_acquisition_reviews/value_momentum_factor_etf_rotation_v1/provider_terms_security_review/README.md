# Provider Terms / Security Review

Subject: `value_momentum_factor_etf_rotation_v1`

This is a provider terms/security review only. No data is downloaded, no API is called, no API key is created or stored, no strategy is implemented, no backtest is run, no paper-forward rule is changed, and no real-money recommendation is made.

Decision: `approve_future_yfinance_download_prompt`.

This decision permits a future, separate download prompt to use the existing project yfinance-compatible path for the six missing ETF factor proxies only, subject to explicit user approval, provider metadata capture, data-quality checks, and raw-data exclusion from compact/advisor evidence.
